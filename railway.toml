[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm db:push --accept-data-loss && pnpm start"
restartPolicyMaxRetries = 5
healthcheckPath = "/"
healthcheckTimeout = 30

[env]
# Alle Environment-Variablen werden in Railway Dashboard konfiguriert
