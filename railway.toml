[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm drizzle-kit push && pnpm start"
restartPolicyMaxRetries = 5
healthcheckPath = "/"
healthcheckTimeout = 30

[env]
# Alle Environment-Variablen werden in Railway Dashboard konfiguriert